name: irisflowerpredictor_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  tflite_flutter: ^0.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/decision_tree_model.tflite
    - assets/knn_model.tflite
    - assets/logistic_model.tflite
    - assets/svm_model.tflite
    - assets/iris_model.tflite
    - assets/images/Iris-setosa.png
    - assets/images/Iris-versicolor.png
    - assets/images/Iris-virginica.png
